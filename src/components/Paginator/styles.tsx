import { css } from "../../../styled-system/css";

export const styles = {
  wrapper: css({
    display: "flex",
    alignItems: "center",
    gap: "{spacing.sm}",
    margin: "{spacing.md}",
  }),
};

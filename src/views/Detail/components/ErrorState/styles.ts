import { css } from "../../../../../styled-system/css";

export const styles = {
  error: css({
    padding: "2rem",
    textAlign: "center",
    color: "red",
    backgroundColor: "rgba(255, 0, 0, 0.1)",
    borderRadius: "4px",
    margin: "1rem",
  }),
};

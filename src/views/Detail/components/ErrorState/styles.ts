import { css } from "../../../../../styled-system/css";

export const styles = {
  error: css({
    padding: "{spacing.xl}",
    textAlign: "center",
    color: "error",
    backgroundColor: "backgroundError",
    borderRadius: "{radii.md}",
    margin: "{spacing.md}",
  }),
};

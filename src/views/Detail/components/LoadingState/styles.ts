import { css } from "../../../../../styled-system/css";

export const styles = {
  loading: css({
    padding: "{spacing.md}",
    textAlign: "center",
    textStyle: "body",
    color: "secondary",
  }),
};

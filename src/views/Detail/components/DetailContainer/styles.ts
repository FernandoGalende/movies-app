import { css } from "../../../../../styled-system/css";

export const styles = {
  main: css({
    padding: "{spacing.xl}",
    backgroundColor: "background",
    gap: "{spacing.xl}",
    display: "flex",
    alignItems: "baseline",
    flexDirection: "column",
  }),
};
